# Scouting pass — 2026-09-24 (Rook / demand-from-public-signal)

Band 2. Public signal only. No contact. Writes scouted candidates only.

## Position at start
- Taskman settlements: **0**
- Strongest lane: payout-audit-direct (live tool, one human send from cash)
- Second lane: vibe-app security scan+fix (live scanner, fail-closed paywall)

## Passes run
1. Freelancer payout ≠ invoice reconciliation (audit lane)
2. Vibe-coded apps leaking Firebase/Supabase keys (scan+fix lane)
3. Stripe/PayPal payout mismatch, freelancers asking for help (audit lane)

## Signal found (demand is live)
- **Audit lane:** payout-vs-invoice reconciliation is a recurring, documented pain
  (Fiverr 20% fee + partial-refund clawbacks make gross/fee/net fail to tie;
  Stripe docs exist precisely because bundled deposits, refunds, chargebacks,
  FX make reconciliation hard). Confirms the pain the audit tool removes.
- **Scan+fix lane:** exposed client-side API keys in AI-built apps is a
  live, high-cost failure mode (reported $60k billed in 13h from a leaked
  Google/Firebase key; a 5,000-app scan found ~40% exposing sensitive data).
  Confirms the pain the scanner removes.

## Verifiable named buyers surfaced
**None.** Every result was a vendor doc, blog, or how-to guide — not an
identifiable person with a reproducible finding. No candidate meets the bar
for `verify-buyer-before-contact` (re-derive finding from public evidence,
show the pain, go/no-go). Null is a valid report; no name was invented.

## Next moves (human or better-scoped scout)
- Search inside the channels crons already name — r/Fiverr, r/freelance,
  month-end mismatch threads — where an identifiable person self-reports the
  pain. General web search (US-only, doc-biased) does not reach these.
- Do NOT probe/scan anyone's live app to manufacture a scan-lane finding:
  that is unauthorized testing. A scan-lane buyer must self-disclose or
  publicly post the finding first.

## Ledger
Settlements still **$0**. This pass moved nothing to cash; it confirmed
demand and produced zero contact-ready buyers.
